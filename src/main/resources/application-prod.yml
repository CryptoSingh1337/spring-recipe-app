spring:
  datasource:
    url: jdbc:mysql://localhost:3306/recipe_prod
    username: recipe_prod_user
    password: recipe_prod

  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false